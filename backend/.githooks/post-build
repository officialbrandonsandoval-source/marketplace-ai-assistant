#!/usr/bin/env sh
set -eu

# Runs after `npm run build:test:push` only if you configure git to use .githooks.
# This hook is optional; see backend/README.md.

cd "$(git rev-parse --show-toplevel)/backend"

npm run type-check
